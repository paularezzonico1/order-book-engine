# order-book-engine

A low-latency limit order book (LOB) matching engine in modern C++17.

This is a work-in-progress scaffold; see commit history for incremental build-out.
Full architecture, complexity, and benchmark documentation lands as the
components are implemented.
